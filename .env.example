# Optional. Absolute site origin used for canonical URLs, sitemap, and Open Graph.
# Leave unset in local development. Set in Vercel once the preview or custom
# domain is known. Do not guess Issy's GoDaddy domain.
#
# Required: no
# Used by: src/content/site.ts, src/app/sitemap.ts, src/app/robots.ts
# NEXT_PUBLIC_SITE_URL=https://example.vercel.app
